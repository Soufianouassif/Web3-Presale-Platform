{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build && mkdir -p public && cp -r dist/public/. public/",
  "outputDirectory": "public",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
